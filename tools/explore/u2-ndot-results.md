# The ṅ-alignment campaign — results against the pre-registered list

Companion to `u2-ndot-preregistration.md` (frozen at d146bff; judged
against THAT list, never re-derived). Every number below is measured by
the instruments named there or by the aliasing-robust companions the
campaign added (each landed as a committed instrument before its number
was used).

## What actually shipped (steps (b)–(d′))

The founding hypothesis — "the chain lacks Driver-1's ṅ" — was REFUTED
by measurement in step (b): the chain's tidal T² was already
Driver-1-consistent (−25.87″/cy² at every age). The measured
decomposition replaced it:

1. **(c) The secular-ë completion** — the certified Lp T³/T⁴ tail
   (v4-D3 derived physics) restored to the deep branch as a bounded
   carrier; dense-DE441 fixed the tail at ×1.0 of Meeus.
2. **(d′) The of-date rate completion** — the chains' J2000 rate gaps
   (−4.20/−22.11/+2.15/−4.50/−6.21 ″/cy, exact closure into the
   argument linears) closed via the DYNAMICAL axial precession (the
   tweakpane day-form beat; p_kin ≡ the H/13 mean by construction) on
   Lp plus self-measured tapered rate anchors on all five of-date
   compositions. 5c then refit non-null (Lp 0.009382° → 0.009082°).

End state: deep ≡ certified snapshot at ≤0.06″/cy (linear) and
≤0.18″/cy² (quadratic) on all five arguments; deep ≡ canon vs 300-epoch
DE441 in const/linear/quadratic; **deep−canon at the −135 instant =
+8.9″** (campaign start: +427″ vs DE441).

## P1–P7, honestly scored

- **P1 (consistency closes)** — frozen method (Part 2, 19 epochs):
  c = +0.365 → 2c = 0.73″/cy², OUTSIDE the frozen 0.0 ± 0.15 band —
  but the method's own sampling noise is ±0.7 (measured by the dense
  companion), wider than the band it was frozen with. The
  aliasing-robust companion (`u2-dense-de441.mjs`, 300 epochs):
  2c = **+0.16 ± 0.13**, effective ṅ = −25.69 — INSIDE the frozen
  −25.83 ± 0.3. Substance: PASS; frozen-method form: miss (band
  tighter than instrument noise).
- **P2 (−135 λ closes to ±50″)** — direct: **+193″** — MISS as
  written. Measured decomposition: the model-owned part (deep−canon)
  is **+8.9″**, inside any band; the remaining ~185″ is the certified
  skeleton's OWN periodic residual vs DE441 at that instant (its
  measured scatter class is 56–73″ RMS) — a canon series-truncation
  term the pre-registration's error model did not contain. β stays
  clean.
- **P3 (required-ΔT enters Stephenson's window)** — **11,275 s,
  INSIDE the published totality window [11,220–12,140]** (baseline
  10,787 → post-(c) 10,994 → post-(d′) 11,275): the primary clause
  PASSES — the two independent reductions now overlap. The
  parenthetic numeric band (11,550 ± 150) misses by 125 s. The
  traditional date remains the unique cascade survivor (z = −0.3,
  latMiss 40 km).
- **P4 (Babylon magnitude ≥ 0.98)** — **0.988** on the traditional
  date: PASS (stretch outright totality: not reached).
- **P5 (theoryDrift collapses)** — −750-era bin: −13.4 → **−2.9 min**
  (band < 2: near-miss by 0.9); effective Δṅ: 1.08 → **0.23″/cy²**
  (band < 0.2: near-miss by 0.03). Direction and order as predicted;
  both bands slightly optimistic.
- **P6 (modern era invariant)** — PASS: centerline mean **8.4″
  reproduced exactly**, max 12.4 → 12.0; canonGeometry 1450/1450
  matched, mean-abs 8.52 min; modern fixtures moved 1–2 ULP (2000) /
  1.4e-4″ (2024); full 19-step chain green.
- **P7 (ancient audit rows improve)** — Babylon bestGap 1,411 →
  **1,269 km**: improved, NOT tier-class (≤~300 km). As pre-registered,
  P7 was conditioned on the PARALLEL scene ground-mapping fix, which
  has not run — the audit metric is scene-side; the physics tier
  (local circumstances, required-ΔT) improved as above.

## Falsification clauses

- Clause 1 (post-alignment dλ > 150″ ⇒ "the gap was not the cause") —
  FIRES as written, and correctly so: step (b) had already refuted the
  single-cause framing. The measured replacement: the gap was THREE
  causes (tail + rates + canon's own instant residual), the first two
  model-owned and now closed to +8.9″, the third not ours.
- Clause 2 (P1 closes but P3 misses the window by > 400 s) — does NOT
  fire: P3 is inside the window.
- Clause 3 (modern gates regress beyond P6 bands) — does NOT fire.

## Verdict

The model-owned ancient-era lunar secular is CLOSED, measured three
independent ways (argument isolation, dense DE441, the −135 instant),
with the modern era invariant and every gate green. The frozen numeric
bands written from the single-cause model scored: 3 pass / 2 near-miss
/ 2 miss-with-measured-attribution — the misses are the pre-registered
error model's incompleteness (canon's own residual; instrument noise
wider than band), each now measured and named, not absorbed. Remaining,
outside this campaign: the canon-vs-DE441 series class (20.3h), and the
scene ground-mapping reconciliation (P7's blocker).
