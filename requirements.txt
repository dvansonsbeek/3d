# Core — used across scripts/ and tools/
#
# Deliberately unpinned: these are research/fitting tools, not shipped
# runtime — the shipped artifacts are hash-stamped, and the check chain's
# freshness gates catch any regeneration drift, whatever the library
# versions. Known-good set (the environment the current fitted artifacts
# were produced with, Python 3.12):
#   numpy 2.4.1 · pandas 3.0.1 · openpyxl 3.1.5 · scipy 1.17.0 · astropy 7.2.0
numpy
pandas
openpyxl
scipy
astropy

# Optional — each used by exactly one script. Uncomment if you run it.
#   rebound     — scripts/nbody_50myr_backward.py  (GPL-3.0; compiled N-body integrator)
#   astroquery  — tools/fit/python/planet_eccentricity_jpl.py
# rebound
# astroquery
