# TODO

## Pending

- **Bounded-Moon completion — deep-time scene phases + the Meeus derivation program** (plan of record: `docs/hidden/IP-bounded-moon-derivation.md`, local). Two arms:
  - **Stage B/C — the Moon rides the ring at every epoch, by construction.** The scene layers are already chain-integrated at deep time (`_dtMoonIntegrator` wiring); the argument skeleton was the frozen side (measured ring-vs-Moon gaps: ΔM′ +44° at +12 kyr, ΔF +119° at +222 kyr — the observed off-ring Moon; Meeus's Lp T⁴ tail alone contributes ~359,000° at +220 kyr). **Always-chains is IMPLEMENTED and ACTIVE (unconditional in deep-time mode — no toggle)** (`_fwMoonArgsDeep` + tools mirror `_fwMoonArgsDeepTools`), enabled by the resolution of **the tropical-month anchor question**: the chain's H-quantized J2000 tropical month is 0.072 s LONGER than Meeus's LPR-implied month (2,360,584.778 vs .706 s) → −0.0146°/cy in Lp → −44 min mean BCE opposition timing, and the timed-Babylonian corpus RESOLVES this 0.07 s/month gap — currently siding with Meeus (chains: mean −44/RMS 54/1-in-band-3 of 6 vs certified +3/36/3). Related: the anomalistic month is 0.171 s SHORTER than Meeus's MPR-implied (ϖ̇ −0.049°/cy — measured, minutes-class only). Also learned: the planetary Lp T² remainder (+7.25″/cy²) is REQUIRED by the record (dropping it = +2 h BCE errors; corrected an earlier 1000× arithmetic slip that called it negligible) — RESOLVED: it now rides the derived bounded carrier `_fwLpPlanetaryCarrier` (see Stage D e_S-scan entry; shipped in 8e131aa). Decision needed: re-derive/reconcile the month anchors vs accept the framework value and let the record arbitrate vs Lp-only delta-chain hybrid. Stage C gates after resolution: divergence meter extended to ±10/50/220 kyr (Moon-on-ring), boundedness scan to ±2 Myr, eclipse self-consistency (eclipses at node crossings at any epoch). Precondition for override-narrowing (below).
  - **Stage C in progress — deep-time Moon-vs-ring gap: fully instrumented, two components remain.** Evidence trail (browser divergence-meter deep rows + probes, tools replica via `_getGraphForProbe`): **(proven consistent)** nodal layer ≡ chain-pred to 0.01° and apsidal layer ≡ chain ≡ args ϖ to a CONSTANT +86.89° at all epochs to +222 kyr; browser args ≡ tools args to 0.01°; the apsidal offset construction is exact (|O| = e·a = 21,104 km; world azimuth = layer θ + Earth's H/13 frame phase, verified to 0.3° at six epochs). **(falsified)** the λβ→RA/Dec conversion-frame hypothesis (two empirical-basis variants) AND the rigid ring-frame placement hypothesis (implemented, measured to be an exact identity — the rotAxis frame is rigid to the base chain; both reverted, certified state bit-restored). **(superseded — both components later resolved, see the Stage D entry)** the "planetary-Lp-T² radial breathing" attribution was FALSIFIED after the carrier shipped (the curve-gap metric is Lp-blind: ϖ = Lp − Mp cancels); the real components were the tools meter's distance-pairing artifact and the browser earth-chain frame divergence, the latter compensated by the ring lock (root cause still open, own TODO item below). Physically-complete follow-up (recorded): re-base the moon stack on the ecliptic-of-date composition so the ring's plane carries the true H/3+H/5 evolution (touches startPos calibrations; the current flat-base ring is the visual approximation).
  - **Stage C correctness verdict (ring vs Moon, component by component — the frame for all remaining work):** secular phases = TIE (same chains, proven); periodic terms (evection/variation/…, 120 series terms) = **Moon** (observed physics the mean ring cannot draw); **mean distance at deep time = RING** — the ring's radius follows Farhat a(t) while the override's distance series carries the hardcoded J2000 mean 385,000.56 km (the override Moon never recedes: ±7.6 km at ±200 kyr, ~76 km at ±2 Myr, unbounded beyond — cheap fix: scale the override distance by a(t)/a₀, in-window ratio 1±1e-7, invisible to certified gates); plane = Moon slightly (ecliptic-of-date-referred β vs the ring's flat-base cone; ≤1° class, resolved by the re-base); planetary-Lp-T² = was record-required in-window but unbounded at deep time — RESOLVED by the shipped bounded carrier, which reconciled both sides exactly as this verdict prescribed. At ±20 kyr ring and Moon agree at the perturbation floor (user-verified observation).
  - **Stage D — derive the remaining Meeus constants** (each replaces its hardcoded value behind the same A/B structure + standard gates), by leverage: D1 the periodic-series amplitudes (60 lon + 60 lat + distance terms; classical theory derives ALL of them from five framework-native small parameters — m = n_sun/n_moon, e_M, e_S = the derived e_E line, sin i, a/a′; first checks: 6.289° = 2·e_M exact, evection ∝ m·e_M, annual equation ∝ m·e_S — start with the six named main terms, gate on % vs Meeus); **D1 STATUS — first-principles derivation ACHIEVED** (`tools/explore/derive-meeus-amplitudes.js`): full Sun–Earth–Moon 3-body integration (RK4, 40–186 yr in ~1–3 s) from framework constants only (GM_EM, mass ratio, GM_sun from AU + sidereal year, e_S H/16 law, e_M, i, month input) — zero literature series, zero fitted coefficients. Results: top-20 longitude amplitudes 100.0±0.1% of Meeus (EoC/evection/variation/annual/parallactic all 100.01–100.07%); 12 latitude amplitudes uniform 99.8% (single common factor — measured as pure amplitude, phase probe 0.003° ≈ 0, window-stable; = D7's open 0.2%); **apsidal period EMERGES at 3232.67 d (input 3231.493, 0.036%) and nodal at 6794.06 d (input 6798.38, 0.064%)** — two of the three Moon inputs derive from the third + solar parameters; also derives a = 386,321 km as the 3-body semi-major for the 27.32166-d month (dissolving the 384,399/385,001/384,748 "which a" family). Two definition-matching calibrations (not fits): e_IC ← the framework's own EoC identity 2e−e³/4 (the 2 ppm result); a_IC ← the month input. Definitional probes: mean osculating i = 5.1459° ≈ the 5.145 input (i-ambiguity only 0.02%); free (demodulated) e = 0.0540 vs mean osculating 0.0554 (Brown's e = 0.0549 sits between — EoC-defined, as the framework's identity assumes). Open residuals: nodal 0.064% + latitude-family 0.2% (missing physics: planetary perturbations, Earth J2, relativity, ecliptic-of-date — all absent from pure 3-body); optionally add J2 to close the gaps. **e_S-SCAN (Stage D × Stage C bridge) — planetary Lp T² DERIVED** (`tools/explore/es-sensitivity-scan.js`): fixed-action Adams–Laplace scan (free e / free i / mean a held at baseline, only e_S varied across the bounded e_E channel range [0.00769, 0.02308]) measures k = ∂n/∂(e_S²) = −2704 °/cy per unit e²; with the derived channel's d(e²)/dT = −1.416e-6/cy at J2000 → **predicted T² = +0.0019144°/cy² (6.89″/cy²) vs the record-required planetary remainder +0.0020131 (7.25″) = 95.1%** — the last labeled-empirical polynomial now has a derived origin (Laplace/Adams reproduced in-framework); the bounded carrier Lp_pl(t) = k·∫(e_E²−e0²)dT matches the T² in-window (−584: 1.29° vs 1.34°) but stays bounded at deep time (200000: 161° vs the T²'s 7892° — a 49× overshoot, the measured Moon-vs-ring breathing driver). Also measured from pure gravity: **s_ϖ = 2.486 (framework Clairaut 2.407, 3.3%) and s_Ω = 0.880 (framework 1.018, 14%)** — first independent numerical confirmation of the T²-channel exponents; E-factor law verified (annual equation ∝ e_S^1.0007, M-free terms exempt at dln/de² ≈ −0.07). Unification candidate: Lp joins the W/N args as rate ∝ g(e_E)^s with s_L ≈ −0.00375 derived (−0.00394 record-normalized) via the existing `_fwChannelIntegral` machinery. **CARRIER SHIPPED (Option A, record-normalized, user-approved; commit 8e131aa):** `_fwLpPlanetaryCarrier(T)` in src/script.js (+ `_fwLpPlanetaryCarrierTools` mirror) replaces (T2_LP − T2_LP_TIDAL)·T² in the deep-args Lp — K_PL = 2·(T2_LP−T2_LP_TIDAL)/(d(e_E²)/dT)₀ = −2843.6 °/cy per e², derived lazily from existing constants (zero new values); Simpson over the e_E² channel, same scheme as `_fwChannelIntegral`. Gates re-run: baseline 0.0015°, J2000 exact, option-C+ ✓, corpus non-deep +3/36/3-of-6 (untouched), deep-chains −8/38/4-of-6 vs certified −6/37/4-of-6 (the predicted ~2-min channel-shape shift, inside noise). Carrier vs old T²: 1800 identical, −584 +0.017°, +52000 232° vs 503°, +200000 169° vs 7892°, +222000 54° vs 9743°. IMPORTANT attribution correction: the moon-on-ring CURVE gap is essentially unchanged by the carrier (52000: 24.7k/38.6k km, radial-dominated ~2·e·a anti-phase; 122000: 9.5k; 222000: 12.9k; planes ≤1.0°) — as expected on reflection, because a pure Lp offset cancels in ϖ = Lp − Mp and shifts the Moon ALONG the shared ellipse, so the shape metric is blind to it. The carrier fixes the Moon's ABSOLUTE deep-time longitude (Sun-relative phase, eclipse geometry — was up to 9,743° wrong); the persisting curve-shape gap is a SEPARATE Stage C item whose driver is now demonstrably NOT the planetary T². **RESOLVED (ring lock shipped, commit 8e131aa):** the full decomposition proved the ring machine exact (canceller pairs 360.000°, offset |O| = e·a, mappings az_perigee = aps.ry + earthOrbit.ry + 90° and az_node = nodal.ry + earthOrbit.ry + 179.3° constant at ALL epochs to ±0.1°), and three root findings: (1) the TOOLS ring already tracks args-ϖ through the of-date conversion exactly (rawA = 0.01° at all epochs); (2) the tools moon-on-ring meter's curve gap is largely a MEASUREMENT ARTIFACT — tools' override modifies only returned RA/Dec while p.distAU stays the RAW pivot distance, so the meter pairs series azimuths with raw anomaly → apparent radial gap = 2·e·a·sin(Δlon/2) (38.3k at 52000 = exactly this); the meter needs a _meeusDistKm-paired override curve (open cleanup); (3) the BROWSER ring is genuinely misaligned (rawA ≈ −155° at 200000) because the browser's Earth H/13 frame chain (earth.orbitObj) diverges from the tools mirror at deep time (~74° at 200k) — the real browser-vs-tools delta, root cause still open (earth-chain deep-time integrator diff). FIX: `_applyMoonRingLock` (src/script.js, called from the Meeus override block) + `_applyMoonRingLockTools` mirror — deep-time-only, re-bases apsidal (+leveling anti-rotation, preserving pair cancellation) and nodal layers onto azWorld(args ϖ/Ω) − θ_earthframe with lazily-calibrated J2000 constants (zero new values; in-window preserved; robust to ANY upstream frame drift incl. the unexplained earth-chain delta). Gates: baseline 0.0015°, J2000 exact, corpus deep −8/38/4-of-6 unchanged, tools lock measured as exact no-op (ring already aligned there). User browser visual check at 200000: CONFIRMED (Moon follows the orbit plane; small residuals = periodic floor + earth-chain delta). Post-lock browser meter: deep curve gaps COLLAPSED 24–73k → 6.5–9.2k km mean; raw-perigee tracks override to 1–2° at all deep epochs. Audit-26 new accepted reference: **1/11/7/0/7** (was 1/12/6/0/7 pre-carrier, 2/10/9/0/5 post-carrier; the lock's scene-state effect moves individual umbra placements both ways; determinism verified — audit-first vs meter-first runs identical; accepted as part of moving to native frame-derived values). L-3 14/14 ±60s and L-4 sanity 14/14 (13/14 types) unchanged certified. Full-canon L-4 new reference: **99.61 / 74.37 / 98.75** (was 99.69/75.42/98.97; all mismatch samples knife-edge at the γ≈1.0/1.5 boundaries; runtime ~1,295 s after the chain-table + drift-cache performance fixes — the scan previously took 92 s/chunk and crashed at chunk 44 on the unbounded `_getJ2000Drift` Map, both fixed). Remaining: root-cause the earth-chain browser-vs-tools delta (affects all planets' deep-time scene positions, not just the Moon). **FAR-FUTURE FIX (commit 8e131aa): Moon stationary at +2.0 Myr / retrograde at +2.1 Myr — NOT physics** (also in production): the cumulative-H table was asymmetric [−500 Myr, +1 Myr] (stale "+700 kyr asymptote" comment — measured: H/LOD/month chains smooth and physical to +500 Myr, LOD 24→27.4 hr, month 27.32→29.42 d), so past +1 Myr `cyclesBetweenYears` returned null and the Moon args silently fell back to the Meeus polynomial skeleton, whose fitted T⁴ tail (−T⁴/65,194,000) cancels the lunar mean motion LPR at T³ = 7.84e12 → **year 1,989,000** exactly (measured: rate 12% of normal at 1.9 Myr, 0 at 2.000 Myr, negative beyond — the polynomial-cliff signature). Fix (both engines): (1) `_CUMUL_INTEGRAL_YEAR_MAX` +1 Myr → **+500 Myr symmetric** (+400 KB table, one-time ms build; paper figures already exercise the physics to ±1 Gyr); (2) fallback `_fwMoonArgs` polynomial tails (T²/T³/T⁴) **clamped at |T| ≤ 100 cy** — beyond the table (>±500 Myr) the Moon degrades to clean prograde mean motion instead of reversing. Verified: prograde at 200k/1.9M/2.0M/2.1M/10M/100M/499M/501M/1000M (22–30°/2d); gates unchanged (baseline 0.0015°, J2000 exact, corpus deep −8/38/4-of-6, option-C+ ✓). Epistemic note (user): the future is now a live prediction surface — model bias (e.g. the LOD-genesis future curvature) extrapolates; the extrapolation-zone labeling in the paper figures already covers this honestly.
  - **"Meeus, but derived" — the framework's own Moon-movement reference document.** Write the definitive technical document presenting the framework's lunar theory in the SAME form Meeus Ch. 47 presents his (so a reader can compute the Moon from it directly), but with every element carrying its derivation instead of a bare fitted value: the three observed inputs (sidereal month 27.32166156 d + apsidal 3231.493 d + nodal 6798.38 d ICRF) and the proof they are not independent (3-body: apsidal/nodal emerge at 0.036%/0.064% given the month); the 8H-lattice counts (35,860,753 / 303,196 / 144,119 per 8H, tablet-arbitrated) and the month table derived from them; the five fundamental arguments in framework-native form (chains + identities + the bounded e_E² carrier for the planetary L′ remainder, K_PL·∫(e_E²−e0²)dT with the Laplace/Adams derivation at 95.1%); the periodic-series tables with their first-principles origin (3-body laboratory: top-20 longitude amplitudes at 100.0±0.1%, latitude family, EoC = 2e−e³/4 at 2 ppm, evection/variation/annual equation from gravity); the E-factor as the derived e_E ratio; deep-time validity (±500 Myr chains, bounded everywhere Meeus's polynomials diverge). Include the laboratory methodology (RK4 setup from framework constants, the two definition-matching calibrations, the fixed-action Adams–Laplace scan) as the experiment log — this absorbs the "Three-Body Laboratory doc" and docs-33/20/41 month-sweep items from the documentation arc. Format: a numbered technical doc in docs/, Meeus-parallel section structure (arguments → E-factor → series → distance → accuracy), date-independent, cross-linked from docs/66 and the website Callout. The ring lock COMPENSATES for it, but the underlying divergence is unexplained: at deep time the browser's Earth H/13 frame (earth.orbitObj) differs from the tools mirror by ~74° at +200 kyr (measured during the Stage C decomposition: browser moon layers ≈ tools layers to 3.5°, yet raw-perigee azimuths differ ~74° → the frame term). Because every planet hangs under the same earth chain, this affects ALL planets' deep-time scene positions, not just the Moon. Instruments ready: the layer-probe pattern (aps.ry/nodal.ry/earthOrbit.ry vs constant mappings) on both sides; next step: dump the browser's earth-chain layer angles at deep epochs (test-button probe) and diff against the tools chain layer-by-layer to find which integrator/convention diverges. ENDGAME (user directive — combine with the ring-lock reversal): the ring lock is a COMPENSATOR, not a root-cause repair (in tools, whose earth chain is consistent, the lock measures as an exact no-op: 0.01° at all epochs). Once the browser earth-chain root cause is fixed, the browser lock must ALSO measure dA/dN ≈ 0 at every epoch — that is the acceptance criterion for the hunt — and the lock is then REVERTED (or demoted to a diagnostic assertion). If the lock doesn't go to zero after the fix, the root cause is not fully found. Full gate battery + visual 200000 check after the reversal.
  - **Tools moon-on-ring meter distance-pairing cleanup.** The tools override modifies only the returned RA/Dec while `p.distAU` stays the RAW pivot distance, so the meter's override curve pairs series azimuths with raw anomaly — an artifact gap of 2·e·a·sin(Δlon/2) (38.3k km at 52000 measured = exactly this formula). Fix: expose the series distance (`_meeusDistKm` equivalent) from computePlanetPosition when the override is active and have the meter use it for the override curve; then the tools meter becomes a faithful mirror of the browser meter. D2 planetary arguments A1/A2/A3 (rates from framework planetary mean motions); D7 latitude 5.128° vs i = 5.145° (now ISOLATED by D1: the classical i×0.99672 splits into dynamics-derived i×0.99458 from pure 3-body + the open uniform 0.2%; not an extraction artifact); D6 s_ϖ = 2.407 from the Hill–Brown e′² series (last anchored sensitivity; falsification path in docs/66 §1); D3 Lp T³/T⁴ tails (candidate: tidal n̈ chain curvature); D5 dissolve the fitted patches (`moonMeeusLpCorrection` 0.010524°, 3-term `MOON_CORRECTION`) as D1's order rises; D4 the remaining 38% equinox-acceleration (research; current stance: framework's own prediction). Still anchored by design: J2000 phase anchors (LP0/D0/M0/MP0/F0 + startPos trio).

- **The 1.2° azimuthal family (node/phase sector)** — three independent ~1.2° tensions, all azimuthal while every magnitude is stiff: (1) reconciling the RAW Souami & Souchay planetary invariable-plane nodes with JPL J2000 ecliptic inclinations moves Earth's node by **−1.23°** (284.51 → 283.28) while i_E moves only +3″ (RMS 93 → 63″, residual ±1′ scatter is intrinsic to the raw S&S set); (2) the lunar e_E channel's value-exact phase sits at **80.09°** vs the derived 81.18° (free-solve attractor 79.96°, A = 0.4936·base); (3) equivalently, Earth's inclination minimum displaced **+339 yr** from the balanced year (the System Reset min-at-balanced-year convention is the only free assumption in the 21.77° anchor chain — the anchor itself is exact lattice arithmetic, ϖ_J2000 cancels). Same size, same sector — possibly one cause. A structural resolution would snap the channel's e(J2000) prediction exact and dissolve the node conflict in one stroke. Derivation context: docs/66 §1 (open research item), `tools/verify/ascending-node-*.js`.

- **Equinox-frame e_E channel line (frame interlock research note)** — the same anchor construction read on the equinox clock (16 = 13 + 3) gives an H/16-period line: rate-solved amplitude = eccentricityAmplitude × 1.046, best-in-class ë prediction (−1.96e-7 vs secular −2.53e-7), e-max at −2726 (channel rate flipping sign in the early-Babylonian era — in-principle testable), mean c = 0.016493 with no framework identity. Exact frame equivalence with the shipped ICRF/H-3 line would require base/amp = 32/3 (actual 11.35 — a 6.4% gap, unexplained). Both pass all current gates; the record separates them only at the ë level. Docs/66 §1 frame-interlock paragraph.

- **Timed Babylonian lunar corpus — verify encodings against Stephenson (1997) Ch. 6** — `tools/explore/timed-babylonian-lunar-eclipses.js` reduces the Almagest-preserved Babylonian records via local astronomy only (no external ΔT, no eclipse canon); current result: framework +3 min mean / 36 min RMS ≈ secular theory's +2/34. The −720/−719/−522 statements are high-confidence; the −501/−490 hour-counts and all magnitude digits need checking against the standard tables before any doc/paper integration. Extension path: Steele's directly-tabulated uš timings (dozens of events) would sharpen the corpus decisively.

- **Override-narrowing implementation ("unmask the Moon", deferred framework-native Moon follow-up)** — the scene Moon's full Meeus RA/Dec override contributes only **0.097°/0.101° RMS** beyond the (now J2000-anchored, framework-native) secular hierarchy — quantified via the unmask-meter button. The implementation arc: narrow the override to a differential periodic correction so the hierarchy speaks for itself (the Sun-unmasking pattern). Scene-side, orthogonal to the shipped eclipse machinery; plan context in `docs/hidden/old-documents/IP-framework-native-moon.md` (archived, local); public derivation record: docs/66 §1.

- **Independent (non-eclipse) amplitude/phase calibration of the ΔT stack + Core-mantle swing** — the periods are zero-fit structural predictions; the amplitudes/phases are capped and jointly eclipse-fitted, so the zero-eclipse-fitting claim applies to the pure-tidal + α(t) chain only. Archeomagnetic calibration is blocked by the known ~300-yr field-model phase-lead systematic (comparative leads 109/186/428 yr across Dumberry & Bloxham 2006 / Wardinski & Korte 2008 / Suttie 2025); watch item: an ArchKalmag14k-based flow/LOD series without the lead. Climate-side path: Bond 1997 IRD / Braun 2005 thermohaline (Bond), Steinhilber ¹⁰Be (Hallstatt), satellite-era-calibrated SIM model (Jose5/Jose4). Full context: [docs/104 §8](docs/104-millennial-rotation-swing.md).

- **The ~12 s irreducible 8H/2024 content** (Stage-E floor diagnostic in `tools/fit/dt-corrections-fit.js`) — real but tiny; EPICA-significant at ~1,326 yr (amp 5.9 ppm vs p95 2.9), Steinhilber marginal. **Joint-world re-measurement 2026-07-23: the line SURVIVES the flip intact — 11.1 s amplitude in the current residual (not absorbed by the swing: different band, 1,325 vs 3,695/3,916 yr), worth only 1 s of full-window RMS (31.2 → 30.2 s), invisible at the ~1,200-s per-event observation noise.** Candidate for a future capped cycle only if independent-archive calibration firms up (the real path: test the 1,325-yr line in Cheng speleothem + further archives beyond EPICA). Companion note: the joint-world L-5b §14 scan's degenerate ~990-yr ridge (hallstatt−jose4 difference-tone band, 8H/2645 = 1,014 yr family, fitted against the smooth Stephenson spline rather than observations) is the same family — research note, NOT a shipping candidate ([docs/104 §8](docs/104-millennial-rotation-swing.md)).

- **Stage 2 (LOD-Climate Rhythm modal): GIA moving-mean analysis** — plan at [docs/hidden/IP-lod-climate-rhythm.md](docs/hidden/IP-lod-climate-rhythm.md) §Stage 2. Rolling-mean deviation over 100/300/1000/3000 yr windows, plus a proposed Framework Climate Index (FCI). Open question: does moving-mean deviation carry signal beyond the raw rate breakdown, or is it a phase-shifted version of the same information?

- **Stage 3 (LOD-Climate Rhythm modal): DT stack tuning sliders** — plan at [docs/hidden/IP-lod-climate-rhythm.md](docs/hidden/IP-lod-climate-rhythm.md) §Stage 3. Per-flag amplitude/phase live-preview sliders (±20% amp, ±50 yr phase) for hypothesis-testing without overwriting the shipped defaults. Less urgent now that Bond IRD correlation validates the calibration (joint world: r = +0.36 validated window / +0.27 full overlap; pre-joint 4-flag stack gave +0.49).

## Closed 2026-07-26 — the fully-derived e_E channel (one H/3 movement)

The lunar perigee/node secular channel and E-factor now run on a FULLY-DERIVED eccentricity line — `e(t) = eccentricityBase · (1 + cos θ(t)/2)`, `θ(t) = 3·(t − balancedYear)/H·360° − 180°` — inputs: Law 5's base, the balanced year, H. Nothing observational enters; e(J2000) (−0.86%), ė (+1.7%), ë (sign-correct), the BCE E-factor (0.4–0.5% of Meeus), and the ±25-kyr rate turning points all emerge as predictions. The day's arc: the H/16 perihelion law tested in the channel and falsified by the record (canon 96.77/44.07/88.98; audit BCE +26…76 min; new timed-Babylonian corpus mean −26/RMS 59 — while landing the closest-ever BCE paths: Babylon 303 km, Halys 122 km → the T²-attribution clue); residual-divisor scan; sec(i) inclination coupling (closure 1.0073, parameter-free) → the single-fluctuation insight (H/3 with the inclination's own phase); amplitude question resolved base/2 (free-solve cross-check 0.4936·base; value-exact 0.561·base rejected as epoch-anchored — the channel is not the osculating e; the curves crossed ~1605); the 21.77° anchor shown EXACT (= ϖ_J2000 − 3·(2000−balY)/H·360 + 180, min-at-balanced-year convention; ϖ cancels from the channel phase). Certified: L-1/L-3/L-5b identical; drift meter M′ +0.221/−135, +0.381/−584 (= predicted-ë divergence, minutes-class); audit-26 split identical 1/12/6/0/7, minutes-class ΔJD, −135 at 1088 km; L-4 99.71/74.88/98.84; tablet corpus +3/36 min ≈ secular theory. Frame interlock discovered (same anchor on the equinox clock → amplitude ≈ eccentricityAmplitude; 16 = 13 + 3) and the 1.2° azimuthal family opened as research items (see Pending). Derivation record: docs/66 §1.

## Closed 2026-07-24 — the framework-native Moon / Path C campaign

The Moon secular-physics asymmetry is fully resolved; `MOON_ARGS_FRAMEWORK_NATIVE` ships default-ON. Stage 0 solved the +1.4°/cy drift (ICRF↔of-date frame convention — the framework's own general precession 360·13/H closes it natively) and found the T² mechanism (e_E solar-perturbation channel: node *derived* at s_Ω = 1.018; perigee one anchored Clairaut sensitivity s_ϖ = 2.407, constant across T²/T³ orders). Stages 1+3 shipped `_fwMoonArgs` + the `_moonArgsAt` dispatcher switch; the T²-nativity arc added the eccentricity composite (lattice + labeled off-lattice 405-kyr g₂−g₅) and *derived* element T³ (meter M′ ≤ 0.015° at −584 vs 1.4°/cy legacy). Stage 4b made the scene hierarchy framework-native: design rate table verbatim, the 5.14° tilt relocated below the nodal spin (plane regresses — measured −19.3549°/yr vs −19.3411 target), draconitic clock = moonNodalMonth (N_drac = N_trop + N_nodI exact), full startPos trio J2000-anchored (Ω/ϖ Δ = 0.0000°; apsidal 347.5544 / nodal 64.0436 / moon 67.8443); prerequisite: legacy −1 removed from the month counts (synodic 0.70 → 0.08 s vs IAU). Item 5 shipped the factored deep-time law (invariant mean × bounded e_E oscillation; perigee ±2% / node ±0.8% over ±2 Myr vs Meeus's unbounded parabola). Item 6 certified the flag-ON baselines: L-4 recall 99.69% / tight 75.42% / type 98.97% (every counter within ±2 events of pure Meeus while bounded at deep time); audit-26 identical 1/12/6/0/7 with −135 at 1040 km (geographic boundary; UT agreement 7 min). Commits 478eeb8 / a647fd6 / 8af3c4f / 3fa694a / 2a1ef87 / 2fc206f / a776a13 + f4d8dbf (release).

Records: `docs/hidden/old-documents/IP-framework-native-moon.md` (plan of record, all stage results, archived, local), docs 99 (invariant frame note + factored law) / 102 (certified baselines) / 103 (−135) / 66 (§1 derivation record + §4 startPos provenance) / 41 (scene architecture + tilt/spin rule); meters kept: drift meter, J2000 element anchoring meter, unmask meter, D/M substitution probe (pure-Meeus A/B via console flag flip). The full campaign chronicle (including the original problem statement) lives in this file's git history.

Visible-orbit frame fix (2026-07-25): the scene ring's distance pattern was observed diverging from the Meeus Moon at earlier epochs; the new "scene orbit vs Meeus divergence" meter (two tables: vs of-date values / vs overridden pivot) attributed it to the apsidal pair running star-referenced (8.8507 yr) while Meeus's perigee is of-date (8.8476) — a frame gap the override cannot absorb for shape orientation. Fix: apsidal + canceller pair switched to the of-date rate (sum-preserving — the pair cancels exactly; the nodal-layer precedent), of-date DT integrator (N_apsI = N_trop − N_anom), startPos re-anchored 347.5544 → 347.5479 (ϖ Δ = 0.0000). Result: visible perigee drift 1.49 → 0.20 °/cy; distance RMS at −584 10,520 → 1,379 km (~the J2000 perturbation floor). Residual = Meeus's perigee T² — the derived e_E-channel deceleration (−0.01032 °/cy² → 6.9° at −584) which the constant-rate layers deliberately omit — bounded, derived, ACCEPTED. Optional future refinement: carry the T² in the layer epoch update. Follow-up: the scene Moon's moveModel block was found carrying a duplicate pure-Meeus argument copy (never framework-native, meaningless at deep time — args wrap 50-140× at ±220 kyr); routed through _moonArgsAt (gates: L-1 identical, divergence meter identical, audit-26 split identical with nearly every ancient BestGap closer — −135 1040 → 1029 km). Deep-time bounded arguments (secular phases from the factored-law chains) = the identified next arc.

Post-campaign completion (2026-07-25): framework-native D and M shipped — identity-composed (D = L′ − L_sun, M = L_sun − ϖ_sun) from the model's real-time year-length integrals, zero new constants, bounded (±5° vs Meeus's 82° at −50 kyr); certified: L-1/L-3/audit-26 identical (−135 = 1040 km), L-4 recall identical with tight recall +13 events better and five boundary-band type flips (129 vs 124, documented ±0.02 band — accepted trade). Derivation record: doc 66 §1; plan archived: `docs/hidden/old-documents/IP-framework-native-dm.md`.

## Closed 2026-07-22/23 — the millennial-rotation-swing / Resonator campaign

The former "identify the ~0.5 ms/cy non-tidal channel" item is fully resolved: the channel is the **millennial core–mantle rotation swing** (Stephenson–Morrison–Hohenkerk's ~1,500-yr oscillation; Dumberry & Bloxham 2006 mechanism), shipped as the **Core-mantle swing** (Resonator driver, 4th dLOD/dt channel) — a lattice-labeled damped core-eigenmode episode (T₀ = 8H/685, Q = 1.8, impulse-consistent kicks −1600/+1600) fitted jointly with the 4-flag stack under a hard USNO closure. Shipped anchors: USNO 86,400.0014 / deltaTStart 56.049 / Espenak RMS 12.60 s / Stephenson full-window 31.3 s (best ever). Joint-world audits: L-5b lunar 20.2 min (117/267 beat NASA; 2-s excess over Stephenson's own fit — the dataset noise floor; §15 anchor drift 6.7 μs/day, §16 rate-equivalent 0.010 ms/cy), L-7 solar 666 s (beats NASA 672 s). All artifacts synced: sim runtime + tweakpane + LOD-Climate modal + dashboard export, website (deepTime.ts calculation layer + doc pages), paper (+ macros), sim docs 99/102/104.

Record of the work: [docs/104](docs/104-millennial-rotation-swing.md) (document of record incl. "The shipped form" and the constraints-resolution), [docs/99](docs/99-expanding-solar-system-resonance-theory.md) + [docs/102](docs/102-gia-alpha-lunar-validation.md) (joint-world refreshed), [scripts/archive/README-resonator-2026-07.md](scripts/archive/README-resonator-2026-07.md) (the 16 campaign scripts + reproduction caveats), `tools/fit/README.md` (authoritative `--joint --write` fit procedure), `tools/fit/validate-resonator.js` (living validation harness). The full campaign chronicle lives in this file's git history (pre-2026-07-23 versions).
