# TODO

## Pending

- **Non-tidal residual — CLOSED as a decomposition (2026-07-22); remaining open pieces below.** The former "identify the ~0.5 ms/cy channel" item is resolved: **no unmodelled physical channel exists**, and every component of the residual now has a named, quantified source. Established by five mutually-confirming experiments in one day:
  - **No uniform secular rate**: solar signed-drift bound (button-17 v3, jackknife-stable) r = −0.13 ± 0.09 ms/cy → constant −0.5 excluded ~4σ
  - **Linear drift ≡ constant LOD-level offset ~730 μs/day** (L-5b §15) — and the 2D epoch sweep (`dt-corrections-fit.js --sweep-epoch`) sharpened it to **δ\* = −750 μs/day, wanted by the ancient window only**: the modern instrumental record (Espenak shape 21.66 s, ΔT(2000) = 63.8 s, observed LOD) pins δ ≈ 0 today. No constant can satisfy both ⇒ **the ~0.75 ms is a time-varying millennial component** (era-structured, ~0 in the telescopic era, ~−0.75 ms/day in antiquity) — consistent with the L-4 per-century wave and the 990 CE bump (§12: symmetric, MWP-scale mass redistribution)
  - **The ~73–85 s "1,326-yr line" is NOT physical** — it is the **USNO anchor's shadow price**: Bond's ~74 s amplitude haircut from closing Σ cycleLOD(2000) onto the USNO target, identical in all three closure formulations tested (Bond-only retained as Espenak-optimal). Stage-E frozen-residual fit finds only **~12 s of irreducible 1,326-yr content**. 5th-cycle toggle: NO-GO. Permanent diagnostics now in `dt-corrections-fit.js`: Stage E + post-closure phantom check (≈75 s = the priced anchor cost)
  - **All five constant fixes closed by experiment**: secular-rate channel ✗ · 5th cycle ✗ · H change ✗ (rotation data boxes **H_J2000 = 335,317 ± ~25 yr**, ~10 μs/day per yr of H — new independent H determination; continuous-H "lattice representative" reframing applied to paper + website 2026-07-22) · USNO endpoint ✗ (pinned by ΔT(2000)) · constant trajectory/epoch offset ✗ (2D sweep)

  **In plain terms:** during the ancient/medieval era Earth's day ran ~0.73–0.75 ms *shorter* than the framework trajectory — ~0.00073 s/day of **missing rotational motion, back then** — integrating to the observed ~700–900 s Babylonian-era ΔT residuals; by the instrumental era the gap is gone (measured), which is what forces the time-varying interpretation. Noteworthy framework-native numeric: **one missing rotation per H cycle = 86400/(H·mSY) = 0.7055 ms/day**, within 3–6 % of the measured offset — the structural sibling of the H/5 missing-motion term (3.527 ms = one day per H/5). Recorded as an observation, not a conclusion: a kinematic counting term is constant, and constants are excluded by the modern window (2D sweep) — it would need a mechanism that switches off between the medieval and instrumental eras.

  **Still open (the genuine last pieces):**
  1. **The ~12 s irreducible 8H/2024 content** (Stage-E floor) — real but tiny; EPICA-significant at ~1,326 yr (amp 5.9 ppm vs p95 2.9), Steinhilber marginal. Candidate for a future capped cycle only if independent-archive calibration firms up
  2. ~~The 14.2-yr peak~~ **RESOLVED 2026-07-22: sampling-window artifact** (`scripts/archive/lod_residual_142yr_window_test.py`). Two independent kills: (a) physical ceiling — the 270 observations span −720..+1280 only (no telescopic era), and a real 0.15-ms decadal LOD line integrates to ~0.12 s of ΔT, four orders below the fitted ~540–590 s peak and the ~1,300 s timing noise; (b) the observation epochs' own spectral window peaks at **14.30 yr** (2nd-strongest structure, part of a ~7.15-yr harmonic comb ≈ the 88-lunation eclipse cycle) — the peak is a property of *when* eclipses were recorded, which is also why it passed the earlier 3/3 subset-robustness checks (all subsets share the catalog cadence). The literature 13.5–13.6-yr core mode (Hsu et al. 2021 J. Geodesy; Ding & Jiang 2024 Sci. China Earth Sci.) is real physics but undetectable at ancient noise; no framework component warranted
  3. ~~Independent (non-eclipse) calibration~~ **RESOLVED 2026-07-22: CONFIRMATION achieved, calibration not achievable** (`scripts/archive/lod_swing_archeomag_calibration.py`). Against the state-of-the-art observational LOD residual (Kiani Shahvandi et al. 2024 GRL / Morrison 2021 Addendum lineage, Zenodo 13885017, now at `data/kiani-shahvandi2024-lod-residual.txt`): our 4-flag stack correlates r = +0.84, **stack + swing r = +0.91** — the swing is real shared signal. Against the *independent geomagnetic* lineage (Rivera, Pavón-Carrasco & Osete 2026 G3 e2025GC012475, SHAWQ-flow ΔLOD predictions, Fig 9a digitized to `data/rivera2026-fig9a-digitized.json`, QC r = 0.82 vs Zenodo): band-filtered lead-scans reproduce the paper's own control (r ≈ 0.62/0.63 @ 280/340-yr lead vs published 0.6 @ 291/315), and **adding the swing improves every comparison** (PT 0.39→0.48, TG 0.44→0.52) — the swing is independently confirmed as core–mantle physics. BUT raw-curve agreement is only ~0.4 and needs a fitted ~300-yr phase lead (a known archeomagnetic systematic: comparative leads 109/186/428 yr for D&B2006/W&K2008/Suttie2025), so **deriving stack amplitudes/phases from flows would degrade the eclipse fit — zero-eclipse-fit via archeomagnetic calibration is out at current field-model quality.** Original shape/identification record (kept for reference): **Independent (non-eclipse) calibration of the millennial time-varying component** — now fully shape-resolved (2026-07-22, production-chain era profile matching L-5b §2 landmarks): **one full aperiodic SWING, not constant, not a harmonic** — real day ~1.8–3.6 ms SHORTER than the model trend through antiquity (−720…+600, Earth fast; already full-strength at record start, onset unobservable), gradual turnaround ~600–1000 CE centered ~900 (the 990 "bump" is this swing's integral), overshoot to ~0.5–1.1 ms LONGER (Earth slow) 1000–1800, converged by the instrumental era. The famous "0.75 ms" is the window-AVERAGE of this swing (−0.67 ms/day computed). **Identification: the aperiodic remainder of the Stephenson-Morrison-Hohenkerk ~1,500-yr ±3–4 ms core-mantle LOD fluctuation** — the Bond flag (1,466 yr) captures its periodic part; caps + anchor haircut leave the aperiodic part as residual. Single lattice cycles excluded (H/46 test: free fit hijacks bump anti-phase, phase-locked fit gets ≈0 amplitude — `scripts/archive/lod_residual_h46_millennial_cycle.py`); the 3-of-3-archive 5.8–8.3 kyr band (`data/lattice-scan-band-3500-15000.json`) is real archive evidence but phase-incompatible with ΔT. **Literature identification confirmed 2026-07-22:** this is the documented **millennial LOD fluctuation** (Stephenson & Morrison "~1,500-yr oscillation"; SMH 2016), physically modeled by **Dumberry & Bloxham 2006 (GJI 165, 32–46)** — archeomagnetic core-flow models independently show episodic east/west flows of the same periodicity whose core-mantle angular-momentum exchange reproduces the ~3–4 ms LOD swing. Framework name: **"millennial rotation swing"** (turnaround ~900 CE); our decomposition = Bond flag carries the quasi-periodic part, the swing is the aperiodic remainder. Amplitude argument excludes climate mass-redistribution for the bulk (2.5 ms ↔ ~1.2 m sea-level equivalent vs observed ≲0.3 m Common-Era → ≤~0.5 ms climatic, plausibly the 990-bump share). Calibration path: **archeomagnetic core-angular-momentum reconstructions** (Dumberry & Bloxham 2006; Rivera et al. 2026 3,300-yr core-surface flows) as primary, plus Bond IRD / Steinhilber ¹⁰Be / speleothems for the climatic share. **Two-engine refinement (2026-07-22):** the amplitude budget requires TWO ~1,500-yr oscillators — the climatic Bond cycle (IRD/temperature; rotational expression ≤ ~0.5 ms, the mass-redistribution ceiling: 3.3 ms would need ~1.6 m SLE vs ≲ 0.3 m observed) and the core-mantle millennial swing (~3–4 ms, the amplitude bulk incl. most of the shipped Bond flag's δLOD). Inseparable inside our 2.7-kyr window (beat ~26 kyr). ~~New falsifiable prediction: Holocene phase-lock between Bond IRD events and archeomagnetic core-flow episodes~~ **TESTED AND FAILED 2026-07-22, before publication** (`scripts/archive/bond_coreflow_phase_lock_test.py`, CFF joint field+flow models of Nilsson et al. jSEDI / ERDA 2776, 9 kyr, derived ΔLOD at `data/cff-coreflow-lod-derived.json`): (a) Bond events quasi-periodic, Rayleigh p ≈ 0.75–0.82 vs the 1,466-yr clock, stack spectrum peaks at ~1,000 + ~2,450 yr (reproducing Obrochta 2012 — note the 2,450-yr peak ≈ our Hallstatt 8H/1104 = 2,430 yr); (b) core-flow ΔLOD episodes: Rayleigh p ≈ 0.9, CFF spectral power at 1,466 yr ≈ 0.004; (c) cross-coherence max r = 0.34 within ±1,000-yr lags, below the phase-randomized null p95 = 0.49. Mainstream "no lock" holds; the 8H/1830 flag is the eclipse-window periodic part of the millennial fluctuation, NOT a Holocene-wide shared clock (caveat: CFF posterior σ ~2 ms limits power before ~2000 BCE). Wording flag: the "ice-mass mediates" narrative stays correct for timing/correlation (r = +0.49) but the ms-scale millennial LOD amplitude is core-supplied — fold into the component-(ii) sharpening
  4. **Paper component-(ii) wording** still says "~0.5 ms/cy fractional non-tidal secular rate, detected but not modelled" — to be sharpened to this decomposition (user decides timing)
  5. **NEW (difference-tone finding, `scripts/archive/lod_swing_difference_tones.py`): the swing = the stack's own rectified beats.** The 8H lattice is closed under difference tones (beat of 8H/n₁ and 8H/n₂ = 8H/(n₁−n₂)). The swing residual is ~98%-representable as the two dominant difference tones of the shipped stack — 8H/726 = 3,695 yr (bond−hallstatt) + 8H/2645 = 1,014 yr (hallstatt−jose4) — a blind 6,328-pair scan selects the same tones; the 1,014-yr phase matches the zero-parameter quadratic-mixing prediction (φ₁−φ₂) within 22° (62 yr); sum tones absent (≤ noise floor) ⇒ low-pass rectification. Constant tones vetoed by modern window ⇒ decaying amplitude — matching the **axisymmetric Magneto-Coriolis eigenmodes of Dumberry, Gerick & Gillet 2025 (GJI 240:2076): predicted periods 1–3 kyr (gravest ~3,000 yr), Q ~ 1–10, episodically re-excited** — the only published wave theory for this band, and it lands on our beat periods (3,530–3,695 yr) and explains the episodicity. Interpretation: core = damped low-pass resonator; lattice beats supply the clock; no published competitor claims astronomical forcing of this band (and no support either — keep kinematic framing). Shipping would need a decay-envelope formalism (not harmonic-only) — deferred; candidate future test: difference-tone phases in climate archives (Bond stack's ~1,000-yr peak ≈ the 1,014-yr tone; its ~2,450-yr peak ≈ Hallstatt). **Follow-up (resonator/excitation/feasibility, `scripts/archive/core_mantle_resonator_fit.py` + `core_mantle_excitation_inversion.py` + `core_mantle_channel4_feasibility.py`):** (a) two independent fit routes select T₀ ≈ 3,800–3,900 yr in the axiMC range (steady A1: T₀=3,900 Q=8, RMS 66 s; ring-down A2: T₀=3,800 Q=1.75, RMS 53 s — both guard-FAIL); (b) excitation inversion: driving is SMOOTH (not impulsive), reverses sign across the ~900 turnaround (+push 730, −pull 1220), landing 7–10 yr from consecutive hallstatt×jose4 envelope extrema (723 max / 1230 min) — p = 0.02 uncorrected, n = 2, partial circularity → drive attribution "as-if" only; (c) channel-4 feasibility: **5th-stack-cycle route DEAD** (modern window caps a constant 1,014-yr tone at 18.5 s vs 59 wanted; absorbs 1.7 s RMS), **4th-force route FEASIBLE** — first-ever guard-passing swing model: fully-decaying composite (episodic coupling), T₀ = 4,000 yr, Q = 0.55 (near-critical, grid edge; decay ~700 yr), locked 1,014-yr drive phase, RMS 118.8 s = 80% of variance, modern 0.15 ms/day / shape 0.79 s / δLOD(2000) 0.098 — all guards pass. Shipping still needs: runtime envelope formalism, excitation-epoch convention (currently launch = record start −720, arbitrary), anchor-closure design. **[SUPERSEDED by Stages 1-2 below — kept for the reasoning trail.]** **STAGE 1 COMPLETE (`scripts/archive/core_mantle_resonator_stage1.py` → `data/core-mantle-resonator-stage1.json`): Core-mantle swing / Resonator driver formalized as a 2-KICK EPISODE, V5 user-confirmed.** Physical-consistency selection rule (shipped T₀ must be the physically determined eigenperiod, pinned 3.8–4.0 kyr — free-T₀ fits are shape parameters, kept as statistical reference only). Key discovery en route: at the physical T₀ a single-kick episode can NEVER pass guards (Q≈1.8 tail still ~0.76 ms/day today) — the swing was actively TERMINATED, exactly the counter-kick the excitation inversion recovered. V5: excitation −1300, termination +1600 (grid edge, caveat), T₀ = 3,950 yr, Q = 1.80, eigen kicks (593/767, 102/−145 s) + bond−hallstatt drive tone (−236 s, phase locked), RMS 53.4 s (≈ unguarded-fit quality 52.7), all guards pass (modern 0.046 ms/day, δLOD(2000) 0.096), raw_at_j2000 = −82.7 s, exact zero before −1300, ±100-kyr safe. Guard-aware solver (modern-window δLOD penalty rows) was required to find these solutions. Tone menu regenerates from ACTIVE flag set (3/4/5-cycle future-proof). Stage 2 next: runtime episode class + default-off toggle in deep-time.js/script.js + exporters (needs per-file approval); Stage 3: validation sweep + kick-epoch stability check. **Interaction network (`scripts/archive/lod_swing_interaction_network.py`): all four flags participate cooperatively — Δn = 34 phase-locking (8H/34 = 78,898 yr) binds bond−hall (726) with jose4−jose5 (760) into one ~3.6-kyr band carrying 93% of the eigenmode-weighted response (engine); hallstatt×jose4's fast 1,014-yr envelope sets forcing-extremum timing (timer); coupling selective, not uniform (F² r = +0.22 vs best pair −0.68, ~6 dof). Full write-up now in doc 104 §6 (resonator formula, excitation inversion, channel-4 feasibility, rejected drivers incl. H/26 with fold caveats).**

- **Core-mantle swing (Resonator driver) — STAGES 1+2 COMPLETE, STAGE 3 + INTEGRATION BACKLOG OPEN.**
  **Stage 1 final (V5, lattice grid, user-confirmed):** episode = 2-kick windowed damped oscillation; **T₀ = 8H/685 = 3,916.1 yr (LATTICE-LABELED — user decision: the shipped resonator is the combined effect of the lattice cycles, so under H(t) evolution the episode must scale WITH its drivers; physical caveat recorded in code: the bare axiMC eigenmode is core-material physics)**; Q = 1.80; excitation −800, termination +1600; kick coefs (763.43/123.33, 98.63/−149.75); bond−hallstatt drive tone −186.14 s phase-locked; RMS 53.4 s; raw@J2000 −82.33 s; δLOD(2000) +0.0988 ms/day; guards all pass. `scripts/archive/core_mantle_resonator_stage1.py` → `data/core-mantle-resonator-stage1.json`. ⚠ Kick-epoch ridge: excitation moved −1300 → −800 between coarse and lattice grids (t_exc/T₀ degeneracy) — Stage 3 must pin it.
  **Stage 2 complete (default-OFF research toggle):** `tools/lib/deep-time.js` (episode class, `DT_RESONATOR_ENABLED=1` opt-in, integrator + Layer-3 + 4-driver decomposition net_L4) + `src/script.js` mirror (`RESONATOR_DT_CORRECTION_ENABLED`, tweakpane dLOD/dt rows "Core-mantle swing" + "+ Core-mantle swing", research toggle button) + `export-dt-corrections.js` RES_* sync (lattice n synced as integer; website targets skip silently until integrated). Verified: default-off bit-identical to pre-change HEAD; enabled matches Stage-1 Python to 1e-14; closure-pending note at the USNO section of `dt-corrections-fit.js`.
  **Stage 3 COMPLETE (constants FROZEN — shipped values unchanged):**
  1. ✅ Kick-epoch stability (`scripts/archive/core_mantle_resonator_stage3_stability.py`): **epochs are NOT data-pinnable** — coordinate refinement ridge-surfs (t₁ bounced −2320/−1220/−1580), stability box t₁ ∈ [−2350, −750] (1,600 yr wide), era jackknife scatters both epochs outside the box, extended-t₂ optimum rides the guard-window edge (+1640), ridge gain only 2.1% (sub-floor). **Shipped (−800, +1600, n = 685, Q = 1.8) STAND AS CONVENTION** (excitation just before record edge = minimal claim; termination 50 yr clear of guard window, Dumberry & Finlay westward-reversal era). Documented uncertainty: t₂ ∈ ~[1300, 1640], n ∈ ~[671, 705], Q ∈ ~[1.5, 2.5]
  2. ✅ USNO closure integrated (`dt-corrections-fit.js` `resonatorLodAtJ2000()`, env-gated `DT_RESONATOR_ENABLED=1`, both closure sites): default runs bit-identical; **resonator-aware dry run: Espenak 21.66 → 17.32 s (improves!), deltaTStart joint optimum 65.92 → 59.16** — the coupled refit (closure shifts Bond → shifts residual → resonator refit iteration) is deliberately deferred to the default-ON decision
  3. ✅ Validation sweep (`scripts/core_mantle_resonator_stage3_validation.js` → `data/core-mantle-resonator-stage3-validation.json`): ALL PASS — ΔT(J2000) = 0 both states; pure LOD untouched; Layer-3 J2000 shift exactly +0.0988 ms/day (the closed item); Stephenson RMS 268.8 → 53.4; Espenak window +0.4 s only; deep time bit-identical ±200 Myr
  4. ✅ `tools/fit/README.md`: stage-1/3 scripts + regeneration rule + resonator-aware closure documented in the Scripts table
  **Default-ON convergence dry run (sandboxed, production restored — `data/core-mantle-resonator-default-on-convergence.json`): iterate-and-close does NOT converge.** Four iterations of [resonator-aware fit → sync → Stage-1 refit] oscillate between two regimes — A: deltaTStart ~58–59, Espenak ~17.1–17.3 s, Bond ~284–287 s, resonator tone ballooning to −1,250…−1,343 s (cancellation-heavy); B: deltaTStart ~65.2–66.0, Espenak ~20.7–20.8 s, Bond ~246–250 s, tone ~−870…−949 s. Diagnosis: Bond (1,466 yr) + the 3,695-yr drive tone + the deltaTStart constant are strongly collinear in the 2.7-kyr window — sequential closure trades amplitude between them. **Default-ON therefore requires a JOINT fit** (stack + resonator in one design matrix with the shared USNO closure) **with resonator amplitude CAPS at the convention values** (tone −186 s — the same cap philosophy as Hallstatt 80 s / Jose5 50 s). The Espenak upside is real in both regimes (17–21 s vs 22.5 production) — the prize exists; it must be collected with the joint-fit design. Design task added below.
  **JOINT-WORLD FLIP EXECUTED (2026-07-23, user: "Definitely flip it") — the Resonator is the shipped 4th driver.** `--joint --write` in `dt-corrections-fit.js` is now the authoritative fit: 4 flags + resonator in one equality-constrained solve. Shipped: **USNO 86400.0026 → 86400.0014** (interior optimum; within 0.2 ms of the observed EO value 86400.0016 the H-recalibration could never reach — the resonator was the missing piece), **deltaTStart 65.92 → 56.05**, **Espenak RMS 21.66 → 12.60 s**, full-window Stephenson RMS → **31.3 s** (best ever). **IMPULSE-CONSISTENT re-ship (same day, user-requested after spotting the −800 ΔT step in the chart): kicks are SIN-ONLY (displacement-continuous — ΔT is accumulated angle and must not step; the cos freedom was unphysical), drive tone SWITCH-ON COMPENSATED, episode = −1600 → +1600 (kick1 sin 760.3 below cap, kick2 −75.2, tone +186.1 at cap); continuity |steps| ≤ 0.014 s vs ~700 s before; cap-creep bug fixed (caps are FIXED convention constants, never derived from the mutable JSON); composite selection (Espenak s.t. full ≤ 40 s) now in the authoritative tool**, Bond 281 → 359.6 s (cos/sin phases moved too), Hallstatt/Jose5 at caps, Jose4 38.9, resonator kick1 at cap 773.3 / **kick2 ≈ 3.5 (termination kick vanishes — medieval shutdown carried by flag interference; doc-104 narrative update pending)** / tone −122.1. Both runtime defaults ON (opt-out `DT_RESONATOR_DISABLED=1`; script.js toggle = research state that breaks anchors); ΔT-integrator master-gate keeps `DT_CORRECTIONS_DISABLED=1` fully raw for fitting. Philosophy shift documented: only the TOTAL is anchor-clean (component-alone δLOD(2000) ≈ +0.786 ms/day); closure verified EXACT (Layer-3 J2000 = 86400.0015000, miss 0.000 μs). Validation sweep ALL PASS (deep time bit-identical ±200 Myr; ΔT(J2000)=0). ⚠ Consequences shifted to backlog: website/paper carry the OLD anchors (86400.0026 / 65.92 / "0.93 ms") until the user syncs (export-to-holistic + text edits); doc 102/104 narratives need the joint-world refresh; historical research scripts that computed "residual after stack" now see the resonator in production mode (use `DT_RESONATOR_DISABLED=1` to reproduce old behavior).
  **Integration backlog (after Stage 3, constants frozen):**
  4b. **Joint-fit design for default-ON — PROTOTYPE GO** (`scripts/archive/core_mantle_joint_fit_stage4.py` → `data/core-mantle-joint-fit-stage4.json`): 4 flags + resonator in ONE equality-constrained solve (USNO closure as hard constraint, exact by construction), amplitude caps at convention values, **resonator phases LOCKED to convention as unit-shape columns with capped scale factors** — the critical design lesson: free resonator cos/sin columns let the cap cascade lock in ballooned phases (full RMS 115 s); phase-locking fixed it. Optimum (proxy-scored on Stephenson 1650–2015): USNO 86400.0024, deltaTStart 72.75, **instrumental RMS 16.0 s (vs production ~22.5) AND full-window RMS 38.2 s (vs sequential 53.4) — better on BOTH windows**, single solve = no ping-pong possible. Bond 334 s; hallstatt/jose5/kick1 at caps; **termination kick nearly drops out (5.3 s) in the joint world** — interpretation note for doc 104. REMAINING before the atomic default-ON flip: re-score the winner under `dt-corrections-fit.js` conventions (true Espenak table, its cap/stage machinery — proxy deltaTStart 72.75 needs the real scoring), integrate a `--joint` mode into the fit tool, then flip both runtimes + exporters + validation sweep
  5. Tweakpane: 4th-layer Solar Day row (with toggle ON the existing `lodReal` row silently becomes Layer 4 — needs an honest separate row); Solar Day decomposition update
  6. LOD-Climate Rhythm modal: resonator curve/overlay option
  7. Dashboard export: 4th-layer solar day
  8. Website (user commits): deepTime.ts RES_* block (exporter picks it up automatically once constants exist), calculator/doc pages (climate-formula, timekeeping), doc 104 §6/§8 refresh with Stage 1-3 numbers
  9. Cosmetic: Jose4 toggle console says "16 total states" (now 32 with 5th flag)
  10. Paper component-(ii) sharpening with the full resonator story (user timing)

- **Stage 2 (LOD-Climate Rhythm modal): GIA moving-mean analysis** — plan at [docs/hidden/IP-lod-climate-rhythm.md](docs/hidden/IP-lod-climate-rhythm.md) §Stage 2. Rolling-mean deviation over 100/300/1000/3000 yr windows, plus a proposed Framework Climate Index (FCI). Open question: does moving-mean deviation carry signal beyond the raw rate breakdown, or is it a phase-shifted version of the same information?

- **Stage 3 (LOD-Climate Rhythm modal): DT stack tuning sliders** — plan at [docs/hidden/IP-lod-climate-rhythm.md](docs/hidden/IP-lod-climate-rhythm.md) §Stage 3. Per-flag amplitude/phase live-preview sliders (±20% amp, ±50 yr phase) for hypothesis-testing without overwriting the shipped defaults. Less urgent now that Bond IRD r = +0.49 validates the current 4-flag calibration.
