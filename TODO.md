# TODO

## Pending

Grouped by the kind of decision each item needs, not by topic: **A** can be picked up today, **B** is open research with no guaranteed outcome, **C** waits on something outside the repo. Items decided or completed move to the dated Closed sections below.

### A · Ready to work — method clear, everything needed is in hand

- **Physical-decomposition runtime re-expression of the channel exponents** (user-approved direction) — re-express s_ϖ/s_Ω as PHYSICAL exponents (2.479/0.867, lab-verified 100.3%/101.5%) + an explicit bounded frame carrier, replacing the Meeus-effective pair (2.407/1.018). Constraint: every gate numerically unchanged by construction (the effective form is exact vs Meeus). Prerequisite thinking done — the obliquity carrier already carries the frame T² for Lp; the args-side needs the same treatment. Record: docs/66 §1.

- **Override-narrowing implementation ("unmask the Moon", deferred framework-native Moon follow-up)** — the scene Moon's full Meeus RA/Dec override contributes only **0.097°/0.094° RMS** (Δlon/Δlat, after mean removal) beyond the (now J2000-anchored, framework-native) secular hierarchy — quantified via the unmask-meter button (Δlat improved from the earlier 0.101 with the dynamical 5.1573° inclination). The implementation arc: narrow the override to a differential periodic correction so the hierarchy speaks for itself (the Sun-unmasking pattern). Scene-side, orthogonal to the shipped eclipse machinery; plan context in `docs/hidden/old-documents/IP-framework-native-moon.md` (archived, local); public derivation record: docs/66 §1.

- **Scene-ring physical completions (Moon, deep time)** — three follow-ups extracted from the completed bounded-Moon program: (1) re-base the moon stack on the ecliptic-of-date composition so the ring's plane carries the true H/3+H/5 evolution (touches startPos calibrations; the current flat-base ring is the visual approximation, ≤1° class); (2) scale the override's distance series by a(t)/a₀ — the ring's radius follows Farhat a(t) but the override Moon never recedes (±7.6 km at ±200 kyr, ~76 km at ±2 Myr, unbounded beyond; in-window ratio 1±1e-7, invisible to certified gates); (3) optional dedicated gate: an eclipses-at-node-crossings self-consistency scan at arbitrary epochs (covered indirectly by option-C+/corpus/audit today). Program record: docs/66 + `docs/hidden/old-documents/IP-bounded-moon-derivation.md`.

- **v4 optional refinements (precision polish, none blocking)** — (1) E5 adiabatic-ramp: plateau windows at integer multiples of the 8.85/18.6-yr cycles to shrink k = −2370 ± 40; (2) A2 amplitude: subtract the analytic first-order drift leak per-sample (Δϖ(t)/ΔΩ(t) measurable from the paired runs) to tighten 101 ± 8%; (3) ṗ composition (104%): longer lab pole run for the track curvature, the 1% π̇ trim, higher-order torque terms (e_moon/e_S evolution in the luni-solar constant). Campaign record: docs/66 + `docs/hidden/old-documents/IP-v4-lab.md`.

### B · Open research — no guaranteed outcome, start with appetite

- **The 1.2° azimuthal family (node/phase sector)** — three independent ~1.2° tensions, all azimuthal while every magnitude is stiff: (1) reconciling the RAW Souami & Souchay planetary invariable-plane nodes with JPL J2000 ecliptic inclinations moves Earth's node by **−1.23°** (284.51 → 283.28) while i_E moves only +3″ (RMS 93 → 63″, residual ±1′ scatter is intrinsic to the raw S&S set); (2) the lunar e_E channel's value-exact phase sits at **80.09°** vs the derived 81.18° (free-solve attractor 79.96°, A = 0.4936·base); (3) equivalently, Earth's inclination minimum displaced **+339 yr** from the balanced year (the System Reset min-at-balanced-year convention is the only free assumption in the 21.77° anchor chain — the anchor itself is exact lattice arithmetic, ϖ_J2000 cancels). Same size, same sector — possibly one cause. A structural resolution would snap the channel's e(J2000) prediction exact and dissolve the node conflict in one stroke. Derivation context: docs/66 §1 (open research item), `tools/verify/ascending-node-*.js`. **Related:** the equinox-frame e_E line below is tension (2) read on the other clock — treat them as one investigation.

- **i-theorem campaign 2 (DLT-1 appendix candidate)** — the analytic compression/rectification theorem needs a leakage-clean estimator (joint LSQ over the full latitude family, integer-multiple windows) + a multi-parameter fit (e², i², m², cross terms); campaign 1 refuted the "(1−m²)" numerology as a parameter coincidence ((3/2)e_M² + sin²i/8 ≈ m² at our Moon) and mapped the anatomy (e² slope ~0.62 nonlinear, i-dep ~sin²i/26, 2e-3 extraction sensitivity) — `tools/explore/v4-i-theorem{,-escan}.js`, docs/66. CONVENTION CAVEAT (user catch, corrected in DLT-1 §3): the catalog moonTilt 6.687° is a CONVENTION-COMPOSED number (Brown/ELP constant 5.1454 + measured ε_ecl 1.5424 = 6.6878) — with the dynamical tilt 5.1573 the same sum reads 6.700°, so the composition must be read in one convention; the independently MEASURED quantity is ε_ecl = 1.5424°. NOT Earth's invariable-plane inclination (misses by 2.2′).

- **Equinox-frame e_E channel line (frame interlock research note)** — the same anchor construction read on the equinox clock (16 = 13 + 3) gives an H/16-period line: rate-solved amplitude = eccentricityAmplitude × 1.046, best-in-class ë prediction (−1.96e-7 vs secular −2.53e-7), e-max at −2726 (channel rate flipping sign in the early-Babylonian era — in-principle testable), mean c = 0.016493 with no framework identity. Exact frame equivalence with the shipped ICRF/H-3 line would require base/amp = 32/3 (actual 11.35 — a 6.4% gap, unexplained). Both pass all current gates; the record separates them only at the ë level. Docs/66 §1 frame-interlock paragraph. **Related:** tension (2) of the 1.2° azimuthal family above — the same e_E channel phase seen from the ICRF side.

### C · Blocked on external inputs — watch items, not work items

- **Timed Babylonian lunar corpus — verify encodings against Stephenson (1997) Ch. 6** — `tools/explore/timed-babylonian-lunar-eclipses.js` reduces the Almagest-preserved Babylonian records via local astronomy only (no external ΔT, no eclipse canon); current certified deep-chains result: −7 min mean / 37 min RMS / 5-of-6 in band (non-deep skeleton +3/36 ≈ secular theory's +2/34). The −720/−719/−522 statements are high-confidence; the −501/−490 hour-counts and all magnitude digits need checking against the standard tables before any doc/paper integration. Extension path: Steele's directly-tabulated uš timings (dozens of events) would sharpen the corpus decisively.

- **Independent (non-eclipse) amplitude/phase calibration of the ΔT stack + Core-mantle swing** — the periods are zero-fit structural predictions; the amplitudes/phases are capped and jointly eclipse-fitted, so the zero-eclipse-fitting claim applies to the pure-tidal + α(t) chain only. Archeomagnetic calibration is blocked by the known ~300-yr field-model phase-lead systematic (comparative leads 109/186/428 yr across Dumberry & Bloxham 2006 / Wardinski & Korte 2008 / Suttie 2025); watch item: an ArchKalmag14k-based flow/LOD series without the lead. Climate-side path: Bond 1997 IRD / Braun 2005 thermohaline (Bond), Steinhilber ¹⁰Be (Hallstatt), satellite-era-calibrated SIM model (Jose5/Jose4) — but that path is now measured and **thin**: against a 95th-percentile permutation threshold all four flags clear EPICA only at R²ₕ ~0.01, Cheng shows nothing for any flag, Bond fails Steinhilber outright (8.5 vs a 23.4 threshold), and Jose4's Steinhilber+EPICA hit cannot count because Jose4 was *identified by* that scan. The Bond↔IRD route is closed as a calibration source: at the Bond period the stack and IRD are ~175° apart ([doc 105 §5](docs/105-dt-stack-flag-audit.md)). **Two archives added 2026-07-28** (`bond_ird`, `gisp2` — the other in-repo candidates are too coarse to resolve 716-2430 yr: CenCO2PIP 100 kyr, Snyder ~470 yr multi-record, Chalk 1.2 Myr), and they change the picture: Hallstatt clears the permutation threshold in Steinhilber, EPICA *and* Bond IRD (1.58 vs p95 1.04, R²ₕ = 0.089, the highest amplitude hit in the scan), Jose5 gains Bond IRD (1.36 vs 1.02), Jose4 stays at 2. **But the PHASE test closes the route.** Counting threshold-clearing archives is the wrong metric — amplitude power in a band is common; phase agreement between archives is what indicates a shared driver. Measured pairwise, the archives disagree: Hallstatt 85°/115°/**160°** across its three (worst of the four flags, all at or above the 90° chance baseline), Jose5 149°, Jose4 52°, Bond 28° on n = 2. **No flag has phase-coherent multi-archive support**, so none is calibratable this way — you cannot fix a phase from sources 160° apart. **And a sharp negative: the Bond harmonic FAILS in Bond's own IRD record** (0.89 vs p95 1.02) while clearing GISP2 — the second independent method to say the 8H/1830 line is not the Bond cycle, after the 175° anti-phase result. The climate-side path is therefore **closed for all four flags**, on phase grounds, and the amplitude side was never transferable anyway: Steinhilber measures solar modulation Φ in MV, and the only attempted Φ→ΔT transfer (`data/hallstatt-steinhilber-fit.json`, Bond-ratio scaling) returns 1,670 s against a shipped cap of 80 s — the conversion is not physically grounded. What remains is the archeomagnetic route, still blocked by the ~300-yr field-model phase lead. Regenerate: `python3 scripts/lattice_harmonic_scan.py --candidates 1830,1104,2989,3749,2024 --datasets all`. Full context: [docs/104 §8](docs/104-millennial-rotation-swing.md).

## Closed 2026-07-29 — the last fitted lunar number, and the archive scan

**The 5.1″ residual: attribution corrected by refutation.** The premise was that the remainder is the correction class ELP/MPP02 embodies, so evaluating `MPP02 − ELP82B` would name it term-by-term. Both halves of that are now measured and both fail. **(1) A sign error inflated the target.** The patch is `Meeus − JPL`, so the +1.13″ truncation enters it NEGATIVE; subtracting it as +1.13 produced the spurious −6.27″. The real remainder is **−4.05″**. **(2) MPP02 does not contain it.** With the series *and* the driver now in `data/lunar-series/elp-mpp02/` (arguments + both Δ-parameter sets, sourced 2026-07-29), MPP02 and ELP82B agree to **0.03″** on this term and 0.30″ RMS in longitude over 2000–2050 — both analytic theories sit together and JPL DE441 sits ~4″ from each. The remainder is an **analytic-vs-numerical** offset, flat across 2000–2050 (3.81/4.07/4.21/4.09″ by sub-window) rather than growing, so it has **no series-term decomposition in any analytic theory** — naming it term-by-term is impossible in principle, not blocked by data. Net: the 5.1″ term is now attributed by CAUSE with both halves measured (−1.13″ named truncation, −4.05″ analytic-vs-DE441), which is a better position than the mislabelled remainder it replaces. No code change: the shipped patch reproduces the measured `Meeus − JPL` to 0.03″, and swapping the Meeus override for MPP02 would remove only the 1.13″ for ~154× the evaluation cost while leaving the 4.05″ still fitted. New: `tools/lib/elp-mpp02.js` (evaluator, 35,901 terms, validated sub-arcsec against full ELP82B 1900–2100), `tools/explore/residual-attribution-mpp02.js`. Records: docs/66 §1, `data/lunar-series/README.md`.

**Two long-standing bucket-D items closed.** *Website conjunction-cycle keys* — **already done**, verified: `model-values` carries `draconicYearEarth` (346.620 d) / `fullMoonCycleEarth`+`…Exact` (411.784 d) alongside the `…ICRF` H/13-frame values, and `reference/appendix.mdx` rows 119-122 show both, labelled "(observed — eclipse year)" vs "(H/13-frame value)". Nothing left to do. *Option B full identifier rename* — **decided: keep the `…ICRF`/`…Earth` names as they are.** The appendix is the working proof that the convention succeeds where it matters: the reader takes meaning from the row label, not the identifier, so a 53-site rename buys no reader clarity while adding breakage surface across the sync scripts — and this session alone turned up several registry/value divergences that mass churn would only multiply. Reopening condition unchanged: if a canonical frame-vocabulary revision ever happens, the 53-finding naming-audit map is in git history at dd72fab.

**8H/2024 stays out — tested across seven archives.** The ~12 s irreducible 8H/2024 content (Stage-E floor diagnostic in `tools/fit/dt-corrections-fit.js`) is real but tiny; EPICA-significant at ~1,326 yr (amp 5.9 ppm vs p95 2.9), Steinhilber marginal. **Joint-world re-measurement 2026-07-23: the line SURVIVES the flip intact — 11.1 s amplitude in the current residual (not absorbed by the swing: different band, 1,325 vs 3,695/3,916 yr), worth only 1 s of full-window RMS (31.2 → 30.2 s), invisible at the ~1,200-s per-event observation noise.** **Cheng tested 2026-07-28 — NEGATIVE, so the promotion condition is not met.** Against the 95th-percentile permutation threshold the 1,325-yr line clears **only EPICA** (5.90 vs p95 2.90); Cheng gives 0.0137 vs 0.0442, Steinhilber now measures as a fail rather than "marginal" (21.48 vs 22.74), Stephenson 359.5 vs 414.1, LR04 unresolvable. That is 1-of-5 — the same marginal profile as the four shipped flags, i.e. no firming-up. The line stays out: the two archives added 2026-07-28 (Bond IRD, GISP2) do **not** change it — 8H/2024 still clears EPICA alone, 1-of-6 resolvable, the weakest of every candidate scanned. Regenerate: `python3 scripts/lattice_harmonic_scan.py --candidates 2024 --datasets all`. Companion note: the joint-world L-5b §14 scan's degenerate ~990-yr ridge (hallstatt−jose4 difference-tone band, 8H/2645 = 1,014 yr family, fitted against the smooth Stephenson spline rather than observations) is the same family — research note, NOT a shipping candidate ([docs/104 §8](docs/104-millennial-rotation-swing.md)).

## Closed 2026-07-28 — the ΔT stack: climate claim downgraded, flag audit passed

Two separate questions were settled, in opposite directions.

**The stack ↔ climate correspondence FAILS its null tests — downgraded everywhere from "cross-validated" to "open correspondence".** The r = +0.36 against Bond 2001 IRD stands as a number (~13 % of variance) but is **not carried by the 1466-yr band**. Four nulls, none significant: band-level sign agreement **65 % of years vs a 50 % coin flip — and 0 % across the whole Little Ice Age**, which the event-level "4 of 5" concealed by sampling one hand-picked year per period and booking the LIA as a single Maunder miss; crossing timing median 262 yr against a Monte-Carlo null of 353 yr (**p ≈ 0.19**), with "10/10 within ±500 yr" near-guaranteed by construction at ~1,170-yr typed crossing spacing; band-limited phase leaving no harmonic significant across 16 tests, the flagship Bond↔IRD pairing sitting at **175°, essentially anti-phase**; and windowed phase-tracking failing to rescue it under a drifting phase (PLV p = 0.49). Reproducible: `tools/explore/climate-crossing-null.js`, `tools/explore/climate-band-phase.js`. Propagated to docs 99/102/50, the modal, `timekeeping.mdx`, `lunar-eclipse-validation.mdx`, `historical-eclipse-validation.mdx`, `predictions.mdx`, `supporting-evidence.mdx`, `analysis-tools.mdx`, the paper, and **both style registries** — which had been instructing writers to say "cross-validated". Also corrected: the two rotation–climate **mechanisms** (surface mass vs solid-Earth GIA) have opposite signs, but only the α(t)↔GIA identification is secure; the stack↔surface-mass one is hypothesis, and presenting it as fact was the incoherence underneath the whole framing.

**The four flags themselves PASS — no flag removed, taper unchanged** ([doc 105](docs/105-dt-stack-flag-audit.md)). Run through `--joint` with pre-registered criteria, the shipped configuration is best in-sample (Espenak RMS **12.60 s** vs 26–35 s for every reduced set) and out-of-sample (split-2 held-out **85.4 s, R² +0.628**, against a 223 s no-correction baseline). Two traps caught, each of which had produced a confidently wrong conclusion first: `fit_metrics.stage_*` in `deltaT-4flag-fit.json` are **legacy cascade diagnostics that rank the flags backwards** (they show Jose4 hurting; under `--joint` it is the largest contributor, 34.89 → 12.60 s) — a `_WARNING_NOT_THE_SHIPPED_FIT` key now ships inside that object; and **Jose5/Jose4 are a coupled pair** (897 / 715.5 yr) that must be judged leave-one-out, never by cumulative cascade. New diagnostic hooks, all refusing `--write`, all byte-identical to the shipped run when unset: `DT_FIT_WINDOW` (train on a sub-window, auto-scores the held-out complement against an intercept-only baseline), `DT_FLAGS`, `DT_FIT_DUMP`. Taper left at ±300/400 kyr: it is observationally inconsequential (stack ΔT bounded at ~±420 s while ΔT grows quadratically — under 0.1 % beyond 10 kyr), so the width is a safety choice, not a derived value. Per-flag archive support measured and marginal: all four clear the EPICA permutation threshold at R²ₕ ~0.01, Cheng shows nothing, **Jose4's Steinhilber hit is circular** (it was *identified by* that scan), and **Bond has the weakest deep-time archive support** despite the best ΔT pedigree.

**LOD-Climate Rhythm modal Stages 2 + 3 — closed, not deferred.** Stage 2's Framework Climate Index rested on Σ_stack's sign being a climate reading; its own open question ("does the moving-mean deviation carry signal beyond the raw rate?") is answered — **r = +0.02** against GISP2, against +0.25 for the accumulated offset, so as a climate index it carries nothing. Stage 3's tuning sliders aimed at "does adjusting a flag improve climate alignment?", which is tuning to the event list — the exact failure this audit exposed — and is superseded by `DT_FLAGS` + `DT_FIT_WINDOW`, which compare configurations by *refitting* rather than by nudging coefficients fitted in each other's presence. What did land from that plan: the band-level sign score computed live beside the event table, the Bond IRD `r` read live instead of hardcoded, the two-mechanism warning, and GIA relabelled two-signed (it is **+0.08 ms/cy at 25,000 BC**, so "a dipping teal line means melting is under way" was simply wrong — the rebound outlasts the melting by millennia). One fragment was considered and dropped by decision: plotting Σ_stack as its own curve (the plan's open question 4) would have made the sign rule visually checkable — worth noting that the sign of what the chart shows disagrees with the sign of the rule **51 % of the time**, since the chart plots rates while Σ_stack is their accumulation — but with the rule itself unvalidated there is nothing to expose. Plan archived to `docs/hidden/old-documents/IP-lod-climate-rhythm.md`.

## Closed 2026-07-27 — The Derived Moon (DLT-1): the Meeus derivation program COMPLETE

**Cassini axial tilt — CLOSED 2026-07-28.** ε_ecl (the measured member of the catalog moonTilt composition) is DERIVED to **0.30%**: full 3-DOF Euler integration of the rigid-body rotation (`tools/explore/moon-euler-rotation.js`; torque from Earth + Sun over the real ELP-2000/82B orbit, RK4, 250 yr) gives **1.5470° vs measured 1.5424°**, from three GRAIL/LLR gravity constants (J₂, C₂₂, C/MR²) + the framework's own month/node/mass-ratio, nothing fitted. Verified start-independent to 0.6″ and step-converged; the averaging lab (`cassini-moontilt.js`) carries a permanent analytic self-test (numerical = exact closed form to 1.00000). Route to the answer, recorded because the negatives were informative: fluid core excluded ~176× and by regime (lunar FCN ≈ 340 yr sits 18× off the 18.6-yr forcing — the suppressing side, unlike Earth's swing at 0.94 of resonance, docs/104); Sun-coherent orbit oscillations only −6.5″; real-orbit ⟨r⁻³⟩ +20.0″ the wrong way; the fixed-axis **averaging assumption itself** −29.3″ = 64% of the gap. Residual 16.5″ (0.30%) = the known unmodelled channels (elastic k₂, degree-3 gravity, two-layer core) — **named, not absorbed; investigation closed by decision.** Framing rule (docs/66, DLT-1, paper, PAPER_STYLE): the Cassini physics is classical (Colombo 1966, Peale 1969) and full integration is standard practice — the claim is only that the framework's constants reproduce a previously-adopted lunar constant. Scene fix shipped: mesh tilt composes i + ε_ecl (6.6997° in scene convention → rendered obliquity = measured 1.5424°); `moonObliquityEclipticJ2000` in astro-reference + export sync + tools mirrors.

Every constant in the framework's lunar theory now carries its origin class: **derived** (periodic amplitudes from gravity at 100.0±0.1%/100.02%; secular T² budget closed against primary sources to 0.08″/cy² with zero free parameters; T³ at 98.8%; ṗ composed at 104% — D4's "38%" identified as the χ-channel; Adams–Laplace k convention-free at −2370±40; analytic aberration), **attributed** (frame/of-date conventions; s-exponents Meeus-effective 2.407/1.018 vs physical 2.479/0.867; i-convention — input replaced with the dynamical mean osculating 5.1573°, Brown/ELP constant 5.1453964 kept as documented partner), **observationally defined** (A1 rate — 18V−16E−M′ hypersensitivity, experimentally proven), **anchored by design** (J2000 phases). Fitted remainder: one 5.1″ raCosMp term + dust — decomposed 2026-07-28 (`tools/explore/residual-attribution-elp.js`): +1.15″ named planetary-series truncation − 6.27″ Meeus-lineage→DE440 ephemeris-generation gap (truncation-artifact reading refuted; attributed in class; naming the gap's terms needs the MPP02 series — optional future). Certified references: baseline 0.0012° (witness RA 222.45959 / Dec −10.90333), corpus −7/37/5-of-6, NASA canon 99.58/74.62/98.66 (knife-edge γ ≈ 1.0/1.5 flips only), audit-26 1/11/6/0/8 (umbra-centerline distance classes, not visibility — high-γ penumbrae still cover "geographic" sites; −708 at 1002 km on the 1000-km boundary), L-3 14/14. Design decisions on record: month anchors = 8H lattice (corpus-arbitrated); Moon chain + args on the TT clock while the Sun/planet `_dtCycleN` layers deliberately stay UT (revisit only if a Sun-side deep-time frame question arises); scene tilt = the physical plane (5.1573°); browser ASTRO_REFERENCE object-literal keys owned by `export-to-script.js` `replaceAstroRefKey`. RECORDS: docs/66 (complete technical record), the website document `reference/the-derived-moon.mdx` ("The Derived Moon", DLT-1 — public capstone), archived plans `docs/hidden/old-documents/{IP-bounded-moon-derivation,IP-v4-lab,session-2026-07-19-jup1740-de441-h-tuning}.md`, and this file's git history through commit 17e669d (the full campaign ledger lived in the Pending bullets; use `git log -p TODO.md` for the play-by-play).

## Closed 2026-07-26 — the fully-derived e_E channel (one H/3 movement)

The lunar perigee/node secular channel and E-factor now run on a FULLY-DERIVED eccentricity line — `e(t) = eccentricityBase · (1 + cos θ(t)/2)`, `θ(t) = 3·(t − balancedYear)/H·360° − 180°` — inputs: Law 5's base, the balanced year, H. Nothing observational enters; e(J2000) (−0.86%), ė (+1.7%), ë (sign-correct), the BCE E-factor (0.4–0.5% of Meeus), and the ±25-kyr rate turning points all emerge as predictions. The day's arc: the H/16 perihelion law tested in the channel and falsified by the record (canon 96.77/44.07/88.98; audit BCE +26…76 min; new timed-Babylonian corpus mean −26/RMS 59 — while landing the closest-ever BCE paths: Babylon 303 km, Halys 122 km → the T²-attribution clue); residual-divisor scan; sec(i) inclination coupling (closure 1.0073, parameter-free) → the single-fluctuation insight (H/3 with the inclination's own phase); amplitude question resolved base/2 (free-solve cross-check 0.4936·base; value-exact 0.561·base rejected as epoch-anchored — the channel is not the osculating e; the curves crossed ~1605); the 21.77° anchor shown EXACT (= ϖ_J2000 − 3·(2000−balY)/H·360 + 180, min-at-balanced-year convention; ϖ cancels from the channel phase). Certified: L-1/L-3/L-5b identical; drift meter M′ +0.221/−135, +0.381/−584 (= predicted-ë divergence, minutes-class); audit-26 split identical 1/12/6/0/7, minutes-class ΔJD, −135 at 1088 km; L-4 99.71/74.88/98.84; tablet corpus +3/36 min ≈ secular theory. Frame interlock discovered (same anchor on the equinox clock → amplitude ≈ eccentricityAmplitude; 16 = 13 + 3) and the 1.2° azimuthal family opened as research items (see Pending). Derivation record: docs/66 §1.

## Closed 2026-07-24 — the framework-native Moon / Path C campaign

The Moon secular-physics asymmetry is fully resolved; `MOON_ARGS_FRAMEWORK_NATIVE` ships default-ON. Stage 0 solved the +1.4°/cy drift (ICRF↔of-date frame convention — the framework's own general precession 360·13/H closes it natively) and found the T² mechanism (e_E solar-perturbation channel: node *derived* at s_Ω = 1.018; perigee one anchored Clairaut sensitivity s_ϖ = 2.407, constant across T²/T³ orders). Stages 1+3 shipped `_fwMoonArgs` + the `_moonArgsAt` dispatcher switch; the T²-nativity arc added the eccentricity composite (lattice + labeled off-lattice 405-kyr g₂−g₅) and *derived* element T³ (meter M′ ≤ 0.015° at −584 vs 1.4°/cy legacy). Stage 4b made the scene hierarchy framework-native: design rate table verbatim, the 5.14° tilt relocated below the nodal spin (plane regresses — measured −19.3549°/yr vs −19.3411 target), draconitic clock = moonNodalMonth (N_drac = N_trop + N_nodI exact), full startPos trio J2000-anchored (Ω/ϖ Δ = 0.0000°; apsidal 347.5544 / nodal 64.0436 / moon 67.8443); prerequisite: legacy −1 removed from the month counts (synodic 0.70 → 0.08 s vs IAU). Item 5 shipped the factored deep-time law (invariant mean × bounded e_E oscillation; perigee ±2% / node ±0.8% over ±2 Myr vs Meeus's unbounded parabola). Item 6 certified the flag-ON baselines: L-4 recall 99.69% / tight 75.42% / type 98.97% (every counter within ±2 events of pure Meeus while bounded at deep time); audit-26 identical 1/12/6/0/7 with −135 at 1040 km (geographic boundary; UT agreement 7 min). Commits 478eeb8 / a647fd6 / 8af3c4f / 3fa694a / 2a1ef87 / 2fc206f / a776a13 + f4d8dbf (release).

Records: `docs/hidden/old-documents/IP-framework-native-moon.md` (plan of record, all stage results, archived, local), docs 99 (invariant frame note + factored law) / 102 (certified baselines) / 103 (−135) / 66 (§1 derivation record + §4 startPos provenance) / 41 (scene architecture + tilt/spin rule); meters kept: drift meter, J2000 element anchoring meter, unmask meter, D/M substitution probe (pure-Meeus A/B via console flag flip). The full campaign chronicle (including the original problem statement) lives in this file's git history.

Visible-orbit frame fix (2026-07-25): the scene ring's distance pattern was observed diverging from the Meeus Moon at earlier epochs; the new "scene orbit vs Meeus divergence" meter (two tables: vs of-date values / vs overridden pivot) attributed it to the apsidal pair running star-referenced (8.8507 yr) while Meeus's perigee is of-date (8.8476) — a frame gap the override cannot absorb for shape orientation. Fix: apsidal + canceller pair switched to the of-date rate (sum-preserving — the pair cancels exactly; the nodal-layer precedent), of-date DT integrator (N_apsI = N_trop − N_anom), startPos re-anchored 347.5544 → 347.5479 (ϖ Δ = 0.0000). Result: visible perigee drift 1.49 → 0.20 °/cy; distance RMS at −584 10,520 → 1,379 km (~the J2000 perturbation floor). Residual = Meeus's perigee T² — the derived e_E-channel deceleration (−0.01032 °/cy² → 6.9° at −584) which the constant-rate layers deliberately omit — bounded, derived, ACCEPTED. Optional future refinement: carry the T² in the layer epoch update. Follow-up: the scene Moon's moveModel block was found carrying a duplicate pure-Meeus argument copy (never framework-native, meaningless at deep time — args wrap 50-140× at ±220 kyr); routed through _moonArgsAt (gates: L-1 identical, divergence meter identical, audit-26 split identical with nearly every ancient BestGap closer — −135 1040 → 1029 km). Deep-time bounded arguments (secular phases from the factored-law chains) = the identified next arc.

Post-campaign completion (2026-07-25): framework-native D and M shipped — identity-composed (D = L′ − L_sun, M = L_sun − ϖ_sun) from the model's real-time year-length integrals, zero new constants, bounded (±5° vs Meeus's 82° at −50 kyr); certified: L-1/L-3/audit-26 identical (−135 = 1040 km), L-4 recall identical with tight recall +13 events better and five boundary-band type flips (129 vs 124, documented ±0.02 band — accepted trade). Derivation record: doc 66 §1; plan archived: `docs/hidden/old-documents/IP-framework-native-dm.md`.

## Closed 2026-07-22/23 — the millennial-rotation-swing / Resonator campaign

The former "identify the ~0.5 ms/cy non-tidal channel" item is fully resolved: the channel is the **millennial core–mantle rotation swing** (Stephenson–Morrison–Hohenkerk's ~1,500-yr oscillation; Dumberry & Bloxham 2006 mechanism), shipped as the **Core-mantle swing** (Resonator driver, 4th dLOD/dt channel) — a lattice-labeled damped core-eigenmode episode (T₀ = 8H/685, Q = 1.8, impulse-consistent kicks −1600/+1600) fitted jointly with the 4-flag stack under a hard USNO closure. Shipped anchors: USNO 86,400.0014 / deltaTStart 56.049 / Espenak RMS 12.60 s / Stephenson full-window 31.3 s (best ever). Joint-world audits: L-5b lunar 20.2 min (117/267 beat NASA; 2-s excess over Stephenson's own fit — the dataset noise floor; §15 anchor drift 6.7 μs/day, §16 rate-equivalent 0.010 ms/cy), L-7 solar 666 s (beats NASA 672 s). All artifacts synced: sim runtime + tweakpane + LOD-Climate modal + dashboard export, website (deepTime.ts calculation layer + doc pages), paper (+ macros), sim docs 99/102/104.

Record of the work: [docs/104](docs/104-millennial-rotation-swing.md) (document of record incl. "The shipped form" and the constraints-resolution), [docs/99](docs/99-expanding-solar-system-resonance-theory.md) + [docs/102](docs/102-gia-alpha-lunar-validation.md) (joint-world refreshed), [scripts/archive/README-resonator-2026-07.md](scripts/archive/README-resonator-2026-07.md) (the 16 campaign scripts + reproduction caveats), `tools/fit/README.md` (authoritative `--joint --write` fit procedure), `tools/fit/validate-resonator.js` (living validation harness). The full campaign chronicle lives in this file's git history (pre-2026-07-23 versions).
