{
  "name": "@essrt/api",
  "version": "0.2.0",
  "private": true,
  "description": "Service surface over the assembled ESSRT model (@essrt/physics createModel) — §7a + Phase 16. One handler, two transports: HTTP (node:http) and MCP (stdio). Build-only until the hosting decision; zero runtime dependencies by design.",
  "type": "module",
  "license": "AGPL-3.0-or-later",
  "exports": {
    ".": "./src/app.js",
    "./server": "./src/server.js",
    "./mcp": "./src/mcp.js",
    "./mcp-server": "./src/mcp-server.js"
  }
}
