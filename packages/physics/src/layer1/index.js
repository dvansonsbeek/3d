/**
 * Derived views — pure, composed only from layer0. 'Kinematic' is f(2000); 'RealLOD' is a division by lodSeconds(year).
 *
 * PHASE 2 SKELETON — empty until the phase noted in IP-technical-design.md §6.
 */
export {};
