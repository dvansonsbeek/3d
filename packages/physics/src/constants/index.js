/**
 * GENERATED at build time from fitted-coefficients.json (§2g). Never hand-edited. Injected via createModel, never imported by a layer.
 *
 * PHASE 2 SKELETON — empty until the phase noted in IP-technical-design.md §6.
 */
export {};
